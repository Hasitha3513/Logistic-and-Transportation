# MVP RC-001 Risk Register

**Task:** MVP-RELEASE-CANDIDATE-001  
**Audit date:** August 23, 2026  
**Scope:** Production-readiness risks for the completed 39-story functional MVP  
**Status:** OPEN — mitigation work is not part of this audit

Likelihood and impact use `LOW`, `MEDIUM`, or `HIGH`. Severity uses `P0` through `P3`. An RC blocker prevents the current commit from being promoted to a release-candidate baseline; a production blocker may be closed during RC hardening but must be closed before production approval.

| Risk ID | Area | Finding | Evidence | Likelihood | Impact | Severity | RC Blocker | Production Blocker | Mitigation | Task ID |
|---|---|---|---|---|---|---|---|---|---|---|
| RC-RISK-001 | Production configuration | The default Spring profile is `h2`; there is no fail-safe production profile. A deployment that omits profile variables can start with an in-memory database and a development JWT default. | `application.yml`, `application-h2.yml`; no `application-prod.yml` | MEDIUM | HIGH | P1 | YES | YES | Add and validate an explicit production profile, require PostgreSQL and secrets, and make accidental H2 startup impossible in production packaging/deployment. | RC-HARDEN-001 |
| RC-RISK-002 | PostgreSQL | Twenty-two PostgreSQL/Testcontainers tests were skipped because Docker is unavailable, including migration, constraint, rollback, allocation, lifecycle, bunker, reading, and offline-sync invariants. | Surefire reports; Docker daemon unavailable | HIGH | HIGH | P1 | YES | YES | Run the complete V1-V29 and concurrency/invariant suites against disposable PostgreSQL 16 and retain evidence. | RC-HARDEN-002 |
| RC-RISK-003 | Release pipeline | No `.github` workflow or other CI/CD pipeline exists, so Java 21, frontend, browser, migration, vulnerability, and artifact gates are not enforced on commits. | No `.github` directory or pipeline configuration | HIGH | HIGH | P1 | YES | YES | Implement a protected CI release pipeline on Java 21 with artifacts and manual production approval. | RC-HARDEN-003 |
| RC-RISK-004 | Runtime support | Local verification compiled for Java 21 but executed on Java 26.0.1; the supported Java 21 runtime was not reproduced outside the unavailable Docker build. | Maven/ArchUnit runtime output; `java -version` | MEDIUM | HIGH | P1 | YES | YES | Reproduce all release gates and packaged startup on Java 21 in CI. | RC-HARDEN-003 |
| RC-RISK-005 | Health/readiness | `/health` always returns `UP` and does not verify PostgreSQL, Flyway, SMTP, or worker readiness. Actuator health is authority-protected and no orchestrator-ready readiness group is configured. | `HealthController`, `application.yml`, `SecurityConfig` | HIGH | HIGH | P1 | NO | YES | Add dependency-aware liveness/readiness groups with narrowly controlled exposure and deployment health checks. | RC-HARDEN-004 |
| RC-RISK-006 | Backup/recovery | No approved PostgreSQL backup frequency, retention, restore test, RPO, RTO, migration recovery, or release rollback procedure exists. | Release documents explicitly leave these items open; no runbooks found | HIGH | HIGH | P1 | NO | YES | Obtain operations decisions and prove backup restore and release rollback in a non-production environment. | RC-HARDEN-005 |
| RC-RISK-007 | Security perimeter | Login/refresh endpoints have no rate limiting, progressive delay, or account lockout; brute-force and token-refresh abuse controls are absent. | No rate-limit/lockout implementation or configuration found | MEDIUM | HIGH | P1 | NO | YES | Define and implement edge/application throttling and auditable authentication abuse controls. | RC-HARDEN-006 |
| RC-RISK-008 | Browser token security | Access and 30-day refresh tokens are stored in `localStorage`, making them accessible to successful same-origin script execution. | `frontend/src/api/client.ts`, `AuthContext.tsx` | MEDIUM | HIGH | P1 | NO | YES | Make an approved threat-model decision; prefer hardened cookie/BFF handling or document compensating CSP/XSS controls. | RC-HARDEN-006 |
| RC-RISK-009 | API capacity | Core vehicle, driver, route, identity, organization, rule, and several report APIs return unbounded lists; `ResourceListPage` paginates these only in the browser. | Controllers/repository ports returning `List`; `ResourceListPage.tsx` | HIGH | HIGH | P1 | NO | YES | Add bounded server pagination and maximum report ranges without changing business semantics. | RC-HARDEN-007 |
| RC-RISK-010 | Query performance | Availability/reporting paths load full Fleet collections and can perform per-record eligibility work; no production query plan or load evidence exists. | `FleetReportReadPort.findAll*`, reporting services, unbounded Fleet endpoints | MEDIUM | HIGH | P1 | NO | YES | Measure production-like query plans, remove proven N+1/full-scan paths, and load-test critical reads. | RC-HARDEN-007 |
| RC-RISK-011 | Data growth | No server-side retention or purge exists for expired refresh tokens, notifications, delivery attempts, rule executions, offline-sync inbox records, histories, or logs. | No cleanup queries/jobs; V2 and V25-V29 append durable data | HIGH | MEDIUM | P1 | NO | YES | Approve retention/legal holds, then implement observable bounded cleanup and supporting indexes using forward changes only. | RC-HARDEN-008 |
| RC-RISK-012 | Observability | Actuator exists, but there are no custom business metrics, structured logging configuration, dashboards, alerts, or metrics for DB, notification, and offline-sync failure/backlog health. | No `MeterRegistry` use or logging configuration; only default endpoints | HIGH | HIGH | P1 | NO | YES | Establish structured logs, safe correlation, metrics, alert rules, and operating dashboards. | RC-HARDEN-004 |
| RC-RISK-013 | Containers | Backend and Nginx containers run as root, base images are not digest-pinned, backend image build skips tests, and frontend build context has no `.dockerignore`. | Root/frontend Dockerfiles; no `frontend/.dockerignore` | MEDIUM | HIGH | P1 | NO | YES | Harden users/filesystems/capabilities, pin approved images, minimize contexts, and test built images in CI. | RC-HARDEN-009 |
| RC-RISK-014 | Local Compose misuse | Compose enables administrator bootstrap and sample data, publishes PostgreSQL, and is a local stack rather than an approved production deployment definition. | `compose.yml` | MEDIUM | HIGH | P1 | NO | YES | Label Compose development-only and create reviewed environment-specific deployment manifests with dev flags forbidden. | RC-HARDEN-001 |
| RC-RISK-015 | SMTP readiness | SMTP validation, TLS modes, timeouts, retries, and sanitized diagnostics exist, but no staging provider handshake/delivery, alerting, or queue-capacity evidence exists; EMAIL is disabled by default. | Notification email configuration/services/tests | MEDIUM | MEDIUM | P2 | NO | YES | Validate real TLS/auth/delivery in staging without production recipients; add backlog metrics and outage runbook. | RC-HARDEN-010 |
| RC-RISK-016 | Frontend resilience | No top-level error boundary or route-level code splitting exists; one 1.88 MB minified JavaScript chunk (566 KB gzip) is produced. | `main.tsx`, `App.tsx`, current Vite build | MEDIUM | MEDIUM | P2 | NO | NO | Add measured lazy route boundaries and a user-safe error boundary after baseline performance measurement. | RC-HARDEN-011 |
| RC-RISK-017 | Frontend quality diagnostics | Unit tests pass but emit Ant Design deprecation/context warnings and MSW unhandled-request diagnostics. | Current `npm test` output | MEDIUM | LOW | P3 | NO | NO | Remove deprecated props and add missing handlers so warnings remain actionable. | RC-HARDEN-011 |
| RC-RISK-018 | Error consistency | Known exceptions use `ApiError`, but there is no catch-all handler; unexpected failures can use Spring's generic response and omit the correlation ID. Client-supplied correlation IDs are not length/character bounded. | `GlobalExceptionHandler`, `CorrelationIdFilter` | MEDIUM | MEDIUM | P2 | NO | YES | Add sanitized unexpected-error handling and validate/generated bounded correlation IDs. | RC-HARDEN-006 |
| RC-RISK-019 | API documentation exposure | Swagger/OpenAPI is public in every non-test profile; no production exposure decision is encoded. | `SecurityConfig` public matchers; no production profile override | MEDIUM | MEDIUM | P2 | NO | YES | Disable by default in production or protect through an explicitly approved administrative boundary. | RC-HARDEN-001 |
| RC-RISK-020 | Supply chain | `npm audit` reports zero current vulnerabilities, but Maven has no configured vulnerability scan, SBOM, image scan, or provenance/signing gate. | npm audit output; `pom.xml`; no CI | MEDIUM | HIGH | P1 | NO | YES | Add Maven, npm, container, secret, and SBOM checks with triage policy in CI. | RC-HARDEN-012 |
| RC-RISK-021 | Performance/capacity | No approved capacity inputs, SLAs, Artillery scenarios, or load-test results exist for critical flows. | No Artillery dependency/config; no capacity document | HIGH | HIGH | P1 | NO | YES | Approve workload/retention inputs and execute safe load tests in an isolated environment. | RC-HARDEN-013 |
| RC-RISK-022 | Privacy | Medical, drug-test, licence, contact, document, and actor data have permission controls, but no data-classification, minimization, encryption, retention, export, or deletion policy is documented. | Fleet DTOs/security mappings; no privacy policy/runbook | MEDIUM | HIGH | P1 | NO | YES | Complete privacy classification and controls review without weakening audit/legal retention. | RC-HARDEN-014 |
| RC-RISK-023 | Deployment edge | Nginx provides SPA routing and API proxying but no TLS termination contract, CSP, HSTS, cache policy, compression, or other explicit security headers. | `frontend/nginx.conf` | MEDIUM | MEDIUM | P2 | NO | YES | Define the trusted ingress boundary and add verified edge security/cache behavior in deployment assets. | RC-HARDEN-009 |
| RC-RISK-024 | Documentation | README and older release documents still describe V10/V11-era scope, obsolete test counts, and features as missing that now exist. | `README.md`, `docs/phase-1-*`, `docs/mvp/MVP-release-readiness.md` | HIGH | MEDIUM | P2 | NO | NO | Reconcile or clearly archive stale release documents after hardening decisions are approved. | RC-HARDEN-015 |
| RC-RISK-025 | Reporting/dashboard | The dashboard endpoint returns only date plus `READY`; driver/vehicle reports are unpaged and may scan broad ranges. This is honest functional behavior but not proven operationally useful or scalable. | `ReportingController`, reporting services | HIGH | MEDIUM | P2 | NO | NO | Obtain product acceptance and include reports in bounded performance tests. | RC-HARDEN-007 |
| RC-RISK-026 | Cross-browser logout | The full Playwright gate left the authenticated dashboard at `/` after **Log out** in Firefox and WebKit (199/201 overall). An isolated rerun passed Firefox but reproduced WebKit, so logout/session-clear completion is not deterministic across supported browsers. | `AuthContext.tsx`, `AppLayout.tsx`, `BasePage.ts`, `auth.spec.ts`; current Playwright output | HIGH | HIGH | P1 | YES | YES | Diagnose the async logout/menu/navigation path, preserve server revocation and local token clearing, add deterministic cross-browser assertions, and rerun all 201 tests with zero failures. | RC-HARDEN-016 |

## Current blocker summary

- **P0:** none found.
- **P1 RC blockers:** RC-RISK-001 through RC-RISK-004 and RC-RISK-026.
- **Production blockers:** the items explicitly marked `YES` above require mitigation or formal risk acceptance before production approval.
- **No committed real secret was identified.** The local `.env` is ignored and was not copied into this document; its values must still be treated as local credentials and rotated if reused elsewhere.
