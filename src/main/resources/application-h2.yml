spring:
  datasource:
    url: jdbc:h2:mem:transport;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
security:
  jwt:
    secret: ${JWT_SECRET:local-development-secret-change-me-32-bytes-minimum}
