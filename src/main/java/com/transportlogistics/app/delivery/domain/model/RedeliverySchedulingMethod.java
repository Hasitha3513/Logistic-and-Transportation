package com.transportlogistics.app.delivery.domain.model;

/**
 * Scheduling method used to determine the delivery window for a re-delivery attempt.
 */
public enum RedeliverySchedulingMethod {
    AUTOMATIC,
    AGENT_ASSISTED
}
