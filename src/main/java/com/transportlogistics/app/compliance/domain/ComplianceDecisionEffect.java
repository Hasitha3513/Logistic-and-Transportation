package com.transportlogistics.app.compliance.domain;

/** Vocabulary only. CS01 defines no rule selecting or aggregating an effect. */
public enum ComplianceDecisionEffect {
    ALLOW,
    ADVISORY,
    RESTRICT,
    BLOCK
}
