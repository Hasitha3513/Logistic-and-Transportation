import { useMutation,useQuery,useQueryClient } from '@tanstack/react-query';import { cargoManifestApi } from '../api/cargoManifestApi';import type { ManifestFilters,ManifestItemPayload } from '../types/cargoManifest';
export const manifestKeys={all:['cargo-manifests'] as const,detail:(id:string)=>['cargo-manifests',id] as const,readiness:(id:string)=>['cargo-manifests',id,'readiness'] as const};
export const useCargoManifests=(filters:ManifestFilters)=>useQuery({queryKey:[...manifestKeys.all,filters],queryFn:()=>cargoManifestApi.search(filters),placeholderData:(old)=>old});
export const useCargoManifest=(id?:string)=>useQuery({queryKey:manifestKeys.detail(id??''),queryFn:()=>cargoManifestApi.get(id!),enabled:Boolean(id)});
export const useManifestReadiness=(id?:string)=>useQuery({queryKey:manifestKeys.readiness(id??''),queryFn:()=>cargoManifestApi.readiness(id!),enabled:Boolean(id)});
export function useManifestActions(id?:string){const client=useQueryClient();const sync=async()=>{if(id){await client.invalidateQueries({queryKey:manifestKeys.detail(id)});await client.invalidateQueries({queryKey:manifestKeys.readiness(id)});}await client.invalidateQueries({queryKey:manifestKeys.all});};return {create:useMutation({mutationFn:cargoManifestApi.create,onSuccess:sync}),addItem:useMutation({mutationFn:(payload:ManifestItemPayload)=>cargoManifestApi.addItem(id!,payload),onSuccess:sync}),updateItem:useMutation({mutationFn:({itemId,payload}:{itemId:string;payload:ManifestItemPayload})=>cargoManifestApi.updateItem(id!,itemId,payload),onSuccess:sync}),finalize:useMutation({mutationFn:(version:number)=>cargoManifestApi.finalize(id!,version),onSuccess:sync})};}
