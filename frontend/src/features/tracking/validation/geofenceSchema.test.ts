import { describe,expect,it } from 'vitest';import { geofenceSchema } from './geofenceSchema';
const polygon=[{longitude:79.8,latitude:6.9},{longitude:79.9,latitude:6.9},{longitude:79.85,latitude:7}];
describe('geofenceSchema',()=>{it('accepts an open WGS84 ring',()=>expect(geofenceSchema.safeParse({name:'Depot',type:'DEPOT',locationId:'location-id',polygon,alertPolicy:{alertOnEntry:true,alertOnExit:false}}).success).toBe(true));it('requires locations for site types and prohibits them for unauthorized zones',()=>{expect(geofenceSchema.safeParse({name:'Depot',type:'DEPOT',polygon,alertPolicy:{alertOnEntry:true,alertOnExit:false}}).success).toBe(false);expect(geofenceSchema.safeParse({name:'Zone',type:'UNAUTHORIZED_ZONE',locationId:'location-id',polygon,alertPolicy:{alertOnEntry:true,alertOnExit:false}}).success).toBe(false)});it('rejects coordinate limits, duplicate closing points, zero area and disabled unauthorized entry alerts',()=>{expect(geofenceSchema.safeParse({name:'Zone',type:'UNAUTHORIZED_ZONE',polygon:[{longitude:181,latitude:0},{longitude:1,latitude:1},{longitude:2,latitude:2}],alertPolicy:{alertOnEntry:false,alertOnExit:false}}).success).toBe(false);expect(geofenceSchema.safeParse({name:'Zone',type:'UNAUTHORIZED_ZONE',polygon:[...polygon,polygon[0]],alertPolicy:{alertOnEntry:true,alertOnExit:false}}).success).toBe(false)});});
