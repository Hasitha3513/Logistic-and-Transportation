import {Button,Space} from 'antd';import {useNavigate} from 'react-router-dom';import {useAuth} from '../../../../auth/AuthContext';
export function DeviationTabs({active}:{active:'rules'|'states'|'episodes'}){const navigate=useNavigate(),{hasPermission}=useAuth();return <Space wrap aria-label="Route deviation sections">{hasPermission('ROUTE_DEVIATION_VIEW')&&<><Button type={active==='states'?'primary':'default'} onClick={()=>navigate('/tracking/route-deviations/states')}>Current state</Button><Button type={active==='rules'?'primary':'default'} onClick={()=>navigate('/tracking/route-deviations')}>Rules</Button></>}{hasPermission('ROUTE_DEVIATION_EVENT_VIEW')&&<Button type={active==='episodes'?'primary':'default'} onClick={()=>navigate('/tracking/route-deviations/episodes')}>Episodes</Button>}</Space>}
