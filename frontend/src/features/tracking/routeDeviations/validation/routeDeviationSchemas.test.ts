import {describe,expect,it} from 'vitest';import {reviewSchema,routeDeviationRuleSchema,validEpisodeRange} from './routeDeviationSchemas';
const base={routeId:'11111111-1111-4111-8111-111111111111',routeVersion:'R1'};
describe('route deviation validation',()=>{it('accepts tolerance boundaries and rejects outside values',()=>{expect(routeDeviationRuleSchema.safeParse({...base,toleranceMeters:10}).success).toBe(true);expect(routeDeviationRuleSchema.safeParse({...base,toleranceMeters:5000}).success).toBe(true);expect(routeDeviationRuleSchema.safeParse({...base,toleranceMeters:9}).success).toBe(false);expect(routeDeviationRuleSchema.safeParse({...base,toleranceMeters:5001}).success).toBe(false)});it('requires a safe UNKNOWN note and bounds all notes',()=>{expect(reviewSchema.safeParse({status:'REJECTED',reason:'UNKNOWN',note:'too short'}).success).toBe(false);expect(reviewSchema.safeParse({status:'REJECTED',reason:'UNKNOWN',note:'unknown reason safely recorded'}).success).toBe(true);expect(reviewSchema.safeParse({status:'APPROVED',reason:'ROAD_CLOSURE',note:'x'.repeat(501)}).success).toBe(false)});it('accepts only ordered ranges no longer than 31 days',()=>{expect(validEpisodeRange('2026-09-01T00:00:00Z','2026-10-02T00:00:00Z')).toBe(true);expect(validEpisodeRange('2026-09-01T00:00:00Z','2026-10-03T00:00:00Z')).toBe(false);expect(validEpisodeRange('2026-09-02T00:00:00Z','2026-09-01T00:00:00Z')).toBe(false)});});
