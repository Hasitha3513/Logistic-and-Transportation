import {zodResolver} from '@hookform/resolvers/zod';import {Alert,Button,Form,Input,InputNumber,Select} from 'antd';import {Controller,useForm,useWatch} from 'react-hook-form';import type {SpeedRule,SpeedRuleInput} from '../types';import {speedRuleSchema,type SpeedRuleFormValues} from '../validation/speedRuleSchema';
export function SpeedRuleEditor({rule,loading,onSubmit}:{rule?:SpeedRule;loading:boolean;onSubmit:(input:SpeedRuleInput)=>Promise<void>}){const {control,handleSubmit,formState:{errors},setValue}=useForm<SpeedRuleFormValues>({resolver:zodResolver(speedRuleSchema),defaultValues:{name:rule?.name??'',scope:rule?.scope??'TENANT',routeId:rule?.routeId??'',routeVersion:rule?.routeVersion??'',thresholdKph:rule?.thresholdKph??80}});const scope=useWatch({control,name:'scope'});return <Form layout="vertical" onFinish={()=>void handleSubmit(value=>onSubmit({...value,routeId:value.routeId||undefined,routeVersion:value.routeVersion||undefined}))()}><Alert type="info" showIcon message="Configured operational threshold" description="Phase 1 uses configured operational thresholds and does not claim live legal road-speed limits." style={{marginBottom:16}}/><Controller name="name" control={control} render={({field})=><Form.Item label="Rule name" required validateStatus={errors.name?'error':undefined} help={errors.name?.message}><Input aria-label="Rule name" {...field}/></Form.Item>}/><Controller name="scope" control={control} render={({field})=><Form.Item label="Rule scope" required validateStatus={errors.scope?'error':undefined} help={errors.scope?.message}><Select aria-label="Rule scope" {...field} disabled={Boolean(rule)} onChange={value=>{field.onChange(value);if(value==='TENANT'){setValue('routeId','');setValue('routeVersion','');}}} options={[{value:'TENANT',label:'Tenant fallback'},{value:'ROUTE_VERSION',label:'Route configuration'}]}/></Form.Item>}/>{scope==='ROUTE_VERSION'&&<><Controller name="routeId" control={control} render={({field})=><Form.Item label="Route ID" required validateStatus={errors.routeId?'error':undefined} help={errors.routeId?.message}><Input aria-label="Route ID" {...field}/></Form.Item>}/><Controller name="routeVersion" control={control} render={({field})=><Form.Item label="Route version" required validateStatus={errors.routeVersion?'error':undefined} help={errors.routeVersion?.message}><Input aria-label="Route version" {...field}/></Form.Item>}/></>}<Controller name="thresholdKph" control={control} render={({field})=><Form.Item label="Configured threshold (km/h)" required validateStatus={errors.thresholdKph?'error':undefined} help={errors.thresholdKph?.message}><InputNumber aria-label="Configured threshold (km/h)" min={0.001} max={400} step={0.1} addonAfter="km/h" value={field.value} onChange={value=>field.onChange(value)}/></Form.Item>}/><Button type="primary" htmlType="submit" loading={loading}>Save rule</Button></Form>}
