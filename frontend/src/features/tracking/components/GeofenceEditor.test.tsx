import { fireEvent,render,screen,waitFor } from '@testing-library/react';import userEvent from '@testing-library/user-event';import { describe,expect,it,vi } from 'vitest';import { GeofenceEditor } from './GeofenceEditor';
describe('GeofenceEditor',()=>{it('supports keyboard-operable add, edit, reorder, remove and submit',async()=>{const user=userEvent.setup();const submit=vi.fn().mockResolvedValue(undefined);render(<GeofenceEditor loading={false} onSubmit={submit}/>);await user.type(screen.getByLabelText('Name'),'Depot');await user.type(screen.getByLabelText('Organization location ID'),'location-id');const add=screen.getByRole('button',{name:'Add Vertex'});add.focus();await user.keyboard('{Enter}');expect(screen.getByLabelText('Vertex 4 longitude')).toBeInTheDocument();fireEvent.change(screen.getByLabelText('Vertex 4 longitude'),{target:{value:'80'}});fireEvent.click(screen.getByRole('button',{name:'Move vertex 4 up'}));fireEvent.click(screen.getByRole('button',{name:'Remove vertex 3'}));fireEvent.click(screen.getByRole('button',{name:'Save geofence'}));await waitFor(()=>expect(submit).toHaveBeenCalledTimes(1));});it('keeps mandatory entry alerts enabled and hides location for unauthorized zones',async()=>{render(<GeofenceEditor loading={false} onSubmit={vi.fn()}/>);fireEvent.mouseDown(screen.getByRole('combobox',{name:'Geofence type'}));fireEvent.click(await screen.findByText('Unauthorized zone'));expect(screen.queryByLabelText('Organization location ID')).not.toBeInTheDocument();expect(screen.getByRole('checkbox',{name:/mandatory/})).toBeDisabled();});});
